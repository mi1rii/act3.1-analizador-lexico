/*
    Ejemplo 1: Programa simple en C
    Demuestra el reconocimiento de palabras clave, identificadores y operadores básicos
*/

#include <stdio.h>

int main() {
    // Declaración de variables
    int count = 0;
    float temperature = 98.6;
    char initial = 'A';
    
    // Comentario de bloque: este es un bucle
    /* El bucle itera 10 veces */
    while (count < 10) {
        printf("Iteración: %d\n", count);
        count = count + 1;
    }
    
    return 0;
}
