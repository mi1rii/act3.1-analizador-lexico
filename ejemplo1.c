#include <stdio.h>

int main() {
    int count = 0;
    float temperature = 98.6;
    char initial = 'A';
    
    while (count < 10) {
        printf("Iteración: %d\n", count);
        count = count + 1;
    }
    
    return 0;
}
